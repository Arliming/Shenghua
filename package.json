{
  "dependencies": {
    "discord-eval.js": "^1.0.10",
    "discord.js": "^12.4.1",
    "godembed": "^1.0.4"
  },
  "main": "index.js",
  "scripts": {
    "send": "git add * && git commit -m update && git push origin master",
    "update": "git pull origin master",
    "deploy": "git pull && npm i && pm2 restart shen && pm2 save"
  }
}
